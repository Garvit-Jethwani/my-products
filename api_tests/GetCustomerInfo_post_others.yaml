# ********RoostGPT********

# Test generated by RoostGPT for test karate-claude_clone_clone using AI Type  and AI Model 
# 
# Test generated for /GetCustomerInfo_post for http method type POST in artillery framework
# RoostTestHash=88810bf424
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  variables:
    authToken: "{{ $processEnvironment.AUTH_TOKEN }}"
  plugins:
    expect: {}

scenarios:
  - name: "GetCustomerInfo API Test"
    flow:
      - post:
          url: "/GetCustomerInfo"
          headers:
            Authorization: "{{ authToken }}"
            botId: "test-bot-id"
            accountId: "test-account-id"
          payload:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 200
          capture:
            json: "$.customerId"
            as: "capturedCustomerId"
      - log: "Captured Customer ID: {{ capturedCustomerId }}"

      - think: 2

      - post:
          url: "/GetCustomerInfo"
          headers:
            Authorization: "{{ authToken }}"
            botId: "test-bot-id"
            accountId: "test-account-id"
          payload:
            customerId: "{{ $randomNumber(1000000, 9999999) }}"
          expect:
            - statusCode: 404
      - log: "Expected 404 status code for invalid customer ID"

      - think: 2

      - post:
          url: "/GetCustomerInfo"
          headers:
            Authorization: "invalid-token"
            botId: "test-bot-id"
            accountId: "test-account-id"
          payload:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 401
      - log: "Expected 401 status code for invalid authorization token"
    payload:
      path: "GetCustomerInfo_post_others.csv"
      skipHeader: true
      delimiter: ","
