# ********RoostGPT********

# Test generated by RoostGPT for test karate-claude_clone_clone using AI Type  and AI Model 
# 
# Test generated for /GetCustomerInfo_post for http method type POST in artillery framework
# RoostTestHash=88810bf424
# 
# 

# ********RoostGPT********
config:
  target: "{{$processEnvironment.API_HOST}}"
  phases:
    - duration: 10
      arrivalRate: 1
      name: Warm up
  payload:
    path: "GetCustomerInfo_post_success.csv"
    fields:
      - "customerId"
    delimiter: "^|^"
    skipHeader: true
  plugins:
    expect: {}
  variables:
    authToken: "{{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Successful Customer Info Retrieval"
    flow:
      - log: "Initiating Customer Info Retrieval"
      - post:
          url: "/GetCustomerInfo"
          headers:
            botId: "abc123"
            accountId: "xyz789"
            Authorization: "Bearer {{ authToken }}"
          payload:
            customerId: "{{ customerId }}"
          expect:
            - statusCode: 
                - 200
            - contentType: json
            - hasProperty: customerId
            - hasProperty: email
            - hasProperty: firstName
            - hasProperty: lastName
            - hasProperty: phone
            - hasProperty: personalBanker
            - hasProperty: personalBanker.name
            - hasProperty: personalBanker.email
            - hasProperty: personalBanker.mobileNumber
            - hasProperty: securityQuestions
            - hasProperty: segmentId
            - hasProperty: userId
            - hasProperty: id
          capture:
            json: "$.customerId"
            as: "capturedCustomerId"
      - log: "Customer Info Retrieved Successfully for customerId={{ capturedCustomerId }}"
      - think: 2
